# WebGAP 项目下一阶段实验与论文级升级要求

## 0. 总体目标

请基于当前仓库、已有代码、已有 checkpoint、已有实验结果以及 `工作与实验说明.md`，继续推进这个科研项目。

当前项目已经完成了第一轮方法验证，但目前最大的科研问题不是代码不能运行，而是：

1. 现有部分 benchmark 太容易，出现明显 ceiling effect。
2. LoRA、WebGAP 等方法在 easy held-out 上大量达到 100%，无法排序。
3. WebSRC 约 92%，也已经接近饱和。
4. 当前 hard_struct 1500 题反而显示 GraphToken 78.7 > LoRA 72.0 > WebGAP 70.6，因此目前还不能形成“WebGAP 整体性能优于简单 baseline”的结论。
5. WebGAP 最强的证据目前是 B5 随机打乱 STAR 后性能从正常水平大幅坍塌，证明结构锚点确实被模型使用，但这更多属于 mechanism evidence，而不是 superiority evidence。
6. hardmix 后 LoRA 也可以达到接近 99%，说明当前 conflict 页上的主要瓶颈可能是 supervision，而不是 GACA/TRB 本身。
7. VisualWebBench 上目前出现负迁移，WebSRC 又接近饱和，因此真实世界 benchmark 的设计还不够理想。

因此下一阶段不要简单地“继续增加实验数量”，而要把项目从：

> “证明一个网页图注意力插件能工作”

进一步升级成：

> **研究多模态大模型在网页视觉结构理解中的结构归因、视觉-DOM 冲突、跨布局泛化以及图结构注入机制，并通过更困难、更真实、更有分辨率的 benchmark 验证 WebGAP。**

最终目标是按照 WWW/The Web Conference 级别科研项目的要求重新组织实验。

---

# 1. 第一优先级：重新设计测试方式，解决指标 ceiling effect

## 1.1 不允许为了“降低指标”而人为修改 benchmark

非常重要：

不要通过以下方式人为制造较低指标：

* 修改 ground truth
* 随机污染测试集
* 故意增加错误标签
* 人为降低模型输入质量而不报告
* 调整 evaluation script 使分数下降
* 随机选择困难样本
* 删除模型容易回答的样本
* 通过不合理 prompt 降低 baseline

目标不是“把 100% 变成 80%”。

真正目标是：

> **寻找真实存在、公开可下载、已有社区使用、且对网页理解/网页视觉结构/网页 grounding 有实际难度的 benchmark。**

优先级：

**真实 benchmark > synthetic benchmark**

**公开 benchmark > 自定义 benchmark**

**已有 benchmark protocol > 自己设计 evaluation metric**

**OOD / compositional / structural conflict > 单纯 OCR QA**

---

# 2. 第二优先级：寻找新的公开 benchmark

请系统调查以下 benchmark 家族，并判断哪些真正适合本项目。

不要直接全部下载。

先做一个 benchmark survey 表：

| Benchmark | Task | 是否有网页截图 | 是否涉及网页结构 | 是否有 HTML/DOM | 数据规模 | 下载大小 | 是否适合 WebGAP | 是否容易饱和 |
| --------- | ---- | ------: | -------: | -----------: | ---: | ---: | ----------: | -----: |

至少重点调查：

### A. WebArena

重点关注：

* 多站点网页环境
* 真实网站
* multi-step web interaction
* DOM / webpage structure
* instruction following
* compositional reasoning

判断是否能够把 WebGAP 从“网页 QA”进一步提升到“web interaction / structural reasoning”。

如果完整环境过重，研究是否可以只下载其公开 task / screenshot / HTML / state subset，用于离线 evaluation。

---

### B. VisualWebArena

重点调查：

* screenshot
* visual grounding
* web navigation
* visual + textual reasoning
* 是否存在结构理解任务

重点判断它是否能够解决当前 VisualWebBench 的问题。

如果可以，优先加入。

---

### C. Mind2Web

重点调查：

* website diversity
* cross-website generalization
* DOM grounding
* action prediction
* HTML / DOM 信息
* screenshot 与 DOM 的关系

特别关注：

> 是否可以把当前 WebGAP 的核心问题重新定义为“视觉页面 + DOM structural grounding”。

如果可行，这是非常重要的 benchmark。

---

### D. WebLINX

重点调查：

* multimodal web interaction
* screenshot
* DOM
* grounding
* action prediction
* unseen website generalization

如果数据规模合理，优先考虑。

---

### E. 其他公开网页 benchmark

继续主动搜索：

* webpage understanding
* webpage screenshot QA
* multimodal web agents
* web grounding
* visual web navigation
* DOM grounding
* webpage layout understanding
* webpage question answering
* screenshot-to-action
* multimodal webpage reasoning

要求：

1. 必须是公开数据。
2. 必须能下载。
3. 不允许自己定义一个全新的 benchmark 冒充公开 benchmark。
4. 如果只是从公开 benchmark 中抽取 subset，必须明确说明“subset evaluation”，不能称为完整 benchmark。
5. 如果数据超过 250GB，原则上放弃。
6. 如果完整数据很大，但存在官方 validation/test subset 或官方 release，可以优先使用官方公开 subset。

---

# 3. 第三优先级：允许下载全量数据，但总数据预算 ≤250GB

重新统计当前所有 benchmark 的真实磁盘需求。

目标：

> **总新增数据控制在 250GB 以内。**

建议优先级：

### Tier A

≤10GB

优先完整下载。

### Tier B

10–50GB

如果 benchmark 与 WebGAP 高度相关，优先完整下载。

### Tier C

50–150GB

只在 benchmark 非常重要时考虑。

### Tier D

> 150GB

除非是极其关键 benchmark，否则不要下载。

---

## 3.1 不允许因为磁盘空间而继续大量使用 synthetic benchmark

目前 WebForge 的作用应该重新定义：

它可以用于：

* controlled experiment
* mechanism analysis
* ablation
* causal test
* structure conflict controlled setting

但是不能继续让 WebForge 承担整个论文的主要 empirical evidence。

论文应该形成：

> Synthetic controlled evaluation
> ↓
> Public benchmark
> ↓
> Cross-domain / OOD evaluation
> ↓
> Mechanistic analysis

而不是：

> Synthetic benchmark
> ↓
> Synthetic benchmark
> ↓
> Synthetic benchmark

---

# 4. 第四优先级：重新设计 evaluation protocol

当前主要问题是：

> 单个页面 + 一个短答案 + EM/F1

非常容易被强大的 MLLM 饱和。

因此需要增加更有分辨率的测试方式。

---

## 4.1 从 single-answer QA 扩展到结构敏感任务

至少考虑以下 evaluation：

### Task 1：Visual Grounding

给：

* screenshot
* question

要求模型：

> 指出答案对应的区域 / element。

评估：

* bbox IoU
* point grounding accuracy
* element grounding accuracy

这样可以真正测试：

> 模型是否知道“这个答案来自页面上的哪个区域”。

这和 STAR 的 geometry-aware design 高度相关。

---

### Task 2：DOM Grounding

给：

* screenshot
* question
* DOM / webpage structure

要求：

> 找到对应 DOM element。

评估：

* element accuracy
* DOM node accuracy
* ancestor accuracy
* sibling/order accuracy

这比单纯 EM 更符合 WebGAP 的核心设计。

---

### Task 3：Visual-DOM Conflict

重点发展。

构造/选择公开数据中天然存在的：

* visual order ≠ DOM order
* spatial proximity ≠ DOM proximity
* caption 与 image 视觉位置不一致
* repeated components
* nested components
* visually similar elements
* hidden / off-screen DOM elements

核心问题：

> **MLLM 到底依赖视觉布局还是 DOM structural relation？**

这是 WebGAP 最值得讲的科研问题之一。

---

### Task 4：Pairwise Structural Ranking

不要只问：

> What is the answer?

可以问：

> Which element comes before A in the DOM?

或者：

> Which card is structurally associated with this image?

然后评估 pairwise accuracy。

这样能够显著降低 ceiling effect。

---

### Task 5：Minimal Structural Perturbation

对于同一网页：

只改变一个结构关系：

* sibling order
* parent-child relation
* element association
* caption association

视觉截图尽量保持不变。

然后测试模型是否改变答案。

这可以形成：

> **Structure Sensitivity Test**

这是非常适合 WWW 的实验。

---

# 5. 第五优先级：加入“结构干预”实验

这是下一阶段非常重要的实验方向。

不要只做：

> original → model → accuracy

而要做：

> original page
> ↓
> controlled structural intervention
> ↓
> model prediction change

至少设计：

### Intervention A：DOM reorder

改变 DOM sibling order。

视觉尽量不变。

测试：

> 模型是否能够识别 DOM order？

---

### Intervention B：Visual reorder

改变视觉布局。

DOM 尽量保持。

测试：

> 模型是否能够识别 visual order？

---

### Intervention C：Cross-modal conflict

视觉和 DOM 给出不同关系。

测试：

> WebGAP / GraphToken / LoRA 分别依赖什么？

---

### Intervention D：Anchor permutation

这就是目前 B5 的升级版本。

不仅随机打乱 STAR，而是设计：

* correct anchor
* local wrong anchor
* globally wrong anchor
* random anchor
* semantic-near anchor

形成一个完整的 degradation curve。

这样比单纯：

> correct vs random

更有论文价值。

---

# 6. 第六优先级：增加 MLLM baseline

现在不能只比较：

* Frozen Qwen3-VL-8B
* LoRA
* GraphToken
* WebGAP

需要增加现代开源 MLLM。

至少加入：

### Qwen 系列

1. Qwen3-VL-8B-Instruct
2. Qwen3-VL-30B-A3B-Instruct
3. Qwen3-VL-32B-Instruct

如果你所说的“Qwen3-VL-3.8-27B”实际对应其他具体 checkpoint，请先确认官方模型名称，不要在代码里写错模型名。

Qwen 官方当前公开的 Qwen3-VL 系列包括 4B、8B、30B-A3B、32B 等版本，因此优先使用官方 checkpoint。

---

### InternVL 系列

至少：

1. InternVL3.5-8B
2. InternVL3.5-14B

资源允许时：

3. InternVL3.5-30B-A3B

InternVL3.5 官方目前提供多个规模，包括 8B、14B、30B-A3B 和 38B，因此可以形成一个很好的跨模型 family comparison。

---

### 其他开源 MLLM

继续调查并至少选择 2–4 个：

* LLaVA-OneVision
* MiniCPM-V
* GLM 系列视觉模型
* InternVL
* Qwen-VL
* 其他当前主流 Apache/MIT/BSD 等许可的开源 MLLM

选择标准不是“模型越多越好”，而是：

> **模型架构和训练范式具有代表性。**

最好形成：

| Family   | Model            | Scale | Architecture | Web benchmark |
| -------- | ---------------- | ----: | ------------ | ------------: |
| Qwen     | Qwen3-VL-8B      |    8B | MLLM         |             ✓ |
| Qwen     | Qwen3-VL-30B-A3B |   MoE | MLLM         |             ✓ |
| InternVL | InternVL3.5-8B   |    8B | MLLM         |             ✓ |
| InternVL | InternVL3.5-14B  |   14B | MLLM         |             ✓ |
| LLaVA    | ...              |   ... | MLLM         |             ✓ |
| MiniCPM  | ...              |   ... | MLLM         |             ✓ |

---

# 7. 非常重要：不要一开始就给所有 MLLM 做 WebGAP adaptation

第一阶段先做：

> **model capability benchmark**

也就是：

不同开源 MLLM：

* zero-shot
* same prompt
* same images
* same questions
* same evaluation

先建立 baseline。

例如：

| Model | Public benchmark A | Public benchmark B | Grounding | Conflict | Structural |
| ----- | -----------------: | -----------------: | --------: | -------: | ---------: |

这样可以回答：

> Web structural reasoning 到底是不是当前 MLLM 的普遍弱点？

如果答案是：

> Qwen / InternVL / LLaVA 等都存在明显 structural weakness

那么 WebGAP 的 scientific motivation 会明显增强。

---

# 8. 第二阶段才做 WebGAP 跨模型迁移

如果工程成本可接受，再考虑：

### Qwen3-VL + WebGAP

作为主实验。

### InternVL3.5 + WebGAP

作为 cross-model validation。

不要求一开始实现所有模型。

至少做到：

> WebGAP architecture is not intrinsically tied to Qwen3-VL.

如果由于 hidden dimension / vision token interface 不同导致适配困难，设计一个统一 adapter：

```text
Vision Tokens
      ↓
Model-specific Projection
      ↓
Shared Structural Adapter
      ↓
GACA / STAR / TRB
      ↓
MLLM
```

重点是：

> 不要为每个模型重新设计完全不同的方法。

否则论文会变成 engineering adaptation。

---

# 9. 第七优先级：重新设计主实验表

最终论文主表不应该再是当前：

> WebHallu-Struct 100%

这种表。

建议主表变成：

### Table 1：Public Web Benchmark

| Method      | Web benchmark A | Web benchmark B | Web benchmark C | Avg |
| ----------- | --------------: | --------------: | --------------: | --: |
| Frozen MLLM |                 |                 |                 |     |
| LoRA        |                 |                 |                 |     |
| GraphToken  |                 |                 |                 |     |
| WebGAP      |                 |                 |                 |     |

---

### Table 2：Structural Reasoning

| Method | Visual Grounding | DOM Grounding | Order | Binding | Multi-hop |
| ------ | ---------------: | ------------: | ----: | ------: | --------: |

---

### Table 3：Cross-Model Generalization

| Model | Frozen | LoRA | GraphToken | WebGAP |
| ----- | -----: | ---: | ---------: | -----: |

---

### Table 4：Structural Intervention

| Method | Original | DOM Reorder | Visual Reorder | Conflict | Random Anchor |
| ------ | -------: | ----------: | -------------: | -------: | ------------: |

这个表会比现在更有科研价值。

---

# 10. 第八优先级：重新定位 WebGAP 的核心 scientific contribution

当前 WebGAP 的故事容易被审稿人理解成：

> “We inject a webpage graph into MLLM attention.”

这个 novelty 不够强。

下一阶段建议把核心问题提升成：

> **How should multimodal LLMs reconcile visual geometry and latent web structure when they disagree?**

也就是：

### Visual structure

和

### Web structural semantics

之间的冲突。

WebGAP 不再只是：

> graph injection module

而应该成为：

> **structure-aware multimodal reasoning mechanism**

---

# 11. 建议进一步发展“Dual Structure”方向

当前实验已经出现一个非常重要的现象：

* GraphToken 对 DOM probe 很强
* WebGAP 的 geometry anchor 确实被使用
* 但 visual ≠ DOM 时，两者存在冲突
* hardmix 后 LoRA 也可以学会 conflict

因此不要回避这个现象。

反而应该把它升级成论文 contribution：

> **Visual structure and DOM structure are complementary but conflicting evidence sources.**

然后研究：

```text
Visual Evidence
      ↓
Spatial Structure
      ↓
       ┐
       ├── Structure-aware Fusion ──→ MLLM
       │
DOM Evidence
      ↓
Semantic Structure
```

而不是简单：

```text
DOM Graph → Attention
```

---

# 12. 可以考虑增加 Conflict-aware Structural Fusion

如果现有代码允许，下一阶段可以实验：

### Visual branch

提供：

* spatial proximity
* BBox
* visual order

### DOM branch

提供：

* parent-child
* sibling
* DOM order
* semantic relation

### Conflict detector

判断：

```text
Visual relation == DOM relation
```

还是：

```text
Visual relation != DOM relation
```

然后动态决定：

```text
w_visual
w_dom
```

例如：

```text
H = α H_visual + (1 - α) H_dom
```

其中 α 根据 conflict/context 动态变化。

不要直接照搬这个公式，而是根据当前 GACA/TRB 代码设计。

---

# 13. 研究一个非常重要的问题：模型什么时候应该相信 DOM？

这是很适合 WWW 的 research question。

例如：

### Case A

Visual order = DOM order

→ 两者一致。

### Case B

Visual order ≠ DOM order

→ 应该根据 task 判断。

### Case C

DOM hierarchy provides information invisible from screenshot

→ DOM 应该更重要。

### Case D

DOM contains irrelevant / hidden / redundant elements

→ visual evidence 应该更重要。

最终形成：

> **Structure Reliability / Structure Conflict Analysis**

这个方向比“graph token injection”更有研究深度。

---

# 14. 增加模型 scaling analysis

如果资源允许，至少：

```text
Qwen3-VL-4B
Qwen3-VL-8B
Qwen3-VL-30B-A3B
Qwen3-VL-32B
```

或者：

```text
InternVL3.5-8B
InternVL3.5-14B
InternVL3.5-30B-A3B
```

研究：

> MLLM scale 增大以后，网页结构理解是否自然变强？

以及：

> WebGAP 的 gain 是否随着模型规模增加而消失？

这是非常有价值的结果。

可能出现：

```text
Small MLLM:
WebGAP +8

Medium MLLM:
WebGAP +4

Large MLLM:
WebGAP +1
```

也可能：

```text
Large MLLM仍然存在结构 conflict
```

无论哪种结果，都比单纯“WebGAP +1.2%”更有研究意义。

---

# 15. 增加 data efficiency experiment

当前 hardmix 结果已经暗示：

> supervision 很重要。

所以应该进一步测试：

```text
1%
5%
10%
25%
50%
100%
```

hard structural training data。

比较：

* LoRA
* GraphToken
* WebGAP

如果 WebGAP 真正有效，希望观察：

> 在少量 structural supervision 下，WebGAP 比普通 LoRA 更 data-efficient。

这会是比最终 accuracy 更好的 contribution。

---

# 16. 增加 OOD / Cross-site generalization

这是 WWW 很重要的一点。

训练：

```text
Site / Template Family A+B+C
```

测试：

```text
Site / Template Family D
```

进一步：

```text
Train websites
↓
unseen website categories
↓
unseen layouts
↓
unseen structural conflicts
```

最终重点报告：

> In-domain performance

以及：

> Cross-site generalization

不要只报告随机 train/test split。

---

# 17. 增加 complexity / efficiency analysis

目前已有：

> +6.8% latency
> +0.05GB memory

这个结果可以保留。

但是继续补：

### Input length

```text
1K
2K
4K
8K
```

### Batch size

```text
1
2
4
8
```

### Metrics

* latency
* peak memory
* throughput
* trainable parameters
* FLOPs（如果容易获得）
* token overhead

最终形成：

> Accuracy vs. Efficiency

而不是单独一个 latency 数字。

---

# 18. 强制执行 statistical evaluation

当前大量实验是 single seed。

下一阶段至少对：

### 主 benchmark

运行：

```text
3 seeds
```

如果计算成本太高：

* 主结果 3 seeds
* 大型 baseline 1 seed
* 明确说明

报告：

```text
mean ± std
```

同时对 WebGAP vs LoRA：

* bootstrap confidence interval
* paired bootstrap test

如果是 paired question-level comparison，优先使用 paired evaluation。

---

# 19. 不要删除当前失败结果

非常重要。

目前：

> GraphToken > WebGAP

这个结果不能偷偷删掉。

应该把它变成 motivation：

> Prompt-level structural representation can sometimes outperform attention-level injection when the model has not been explicitly trained on visual-DOM conflicts.

然后提出：

> WebGAP 的目标不是简单把 graph 塞进 attention，而是研究如何在 multimodal structural conflict 下选择和融合结构证据。

这样论文故事会比单纯隐藏失败结果强很多。

---

# 20. 当前实验中哪些内容应该降级

以下内容不要再作为论文核心贡献：

### 不要作为主结果

* easy held-out 100%
* leakage 100%
* H1–H5 全满
* WebSRC 92%
* −GACA / −TRB 在 easy set 上 100%
* ERPR 99.8 vs 100%

这些可以：

> appendix / diagnostic experiment

---

# 21. 当前实验中哪些内容应该保留

### 必须保留

#### B5 STAR permutation

这是当前最好的 causal/mechanistic evidence。

尤其保留：

```text
correct anchor
vs
local permutation
vs
random permutation
```

最好升级成 degradation curve。

---

### hard_struct 1500

保留。

但不要把 WebGAP 70.6 包装成 superiority。

它的作用应该变成：

> structural diagnostic benchmark

---

### GraphToken

必须保留。

因为它是目前非常强的 baseline。

不能弱化它。

真正的问题应该变成：

> Why does explicit graph serialization outperform attention injection on some structural tasks?

然后研究这个问题。

---

# 22. 最终希望形成的论文故事

最终不要写成：

> We propose WebGAP, a graph attention module, which improves webpage QA.

建议向下面这个方向演化：

> **Multimodal LLMs possess strong visual webpage understanding, but their reasoning over webpage structure remains fragile when visual geometry and latent DOM structure disagree.**

然后：

```text
Observation
↓
Current MLLMs are strong on ordinary webpage QA
↓
but weak on structural attribution / DOM grounding / visual-DOM conflict
↓
Existing benchmarks are often saturated
↓
We establish a more challenging structural evaluation
↓
We analyze how MLLMs use visual and DOM evidence
↓
We introduce WebGAP as a structure-aware attention mechanism
↓
We conduct controlled intervention
↓
We conduct cross-model / cross-site / OOD evaluation
↓
We analyze when graph injection helps and when it fails
```

这个故事比：

> “我们的插件提高了 1% accuracy”

要强得多。

---

# 23. Cursor 实际执行顺序

不要一次把所有实验都跑。

严格按照下面顺序执行。

## P0：Benchmark Survey

先不要训练。

完成：

1. 搜索公开网页 benchmark。
2. 统计数据规模。
3. 统计下载大小。
4. 统计 license。
5. 统计 task。
6. 判断是否有 screenshot。
7. 判断是否有 HTML/DOM。
8. 判断是否有 grounding。
9. 判断是否存在结构推理。
10. 判断是否存在 ceiling effect。

输出：

```text
docs/BENCHMARK_SURVEY.md
```

---

## P1：下载最有价值的 benchmark

总新增数据：

> ≤250GB

优先选择：

1. 一个 visual web benchmark
2. 一个 DOM / structural web benchmark
3. 一个 web interaction benchmark
4. 一个 cross-site / OOD benchmark

不要重复下载高度相似的数据。

---

## P2：统一 evaluation interface

实现：

```text
evaluate.py
```

支持：

```text
Qwen3-VL
InternVL3.5
other MLLMs
```

统一：

* image
* prompt
* answer
* generation
* EM
* F1

进一步统一：

* grounding
* structural
* conflict
* intervention

---

## P3：先跑 MLLM baseline zoo

不要先跑 WebGAP。

先跑：

```text
Qwen3-VL-8B
Qwen3-VL-30B-A3B
Qwen3-VL-32B
InternVL3.5-8B
InternVL3.5-14B
+ 2–4 other representative open MLLMs
```

先建立：

> 当前 MLLM 在 structural web reasoning 上到底有多强。

---

## P4：重新跑现有 baseline

至少：

```text
Frozen
LoRA
GraphToken
WebGAP
```

全部使用完全相同的：

* input
* prompt
* benchmark
* evaluation script

---

## P5：Structural intervention

实现：

```text
DOM reorder
Visual reorder
Visual-DOM conflict
Anchor permutation
```

这是最高优先级的新实验。

---

## P6：WebGAP ablation

至少：

```text
-GACA
-TRB
-STAR
-random STAR
-visual only
-DOM only
-dual
-dual + conflict
```

注意：

所有 ablation 必须重新训练还是 inference-only，需要根据实验逻辑决定。

不能用不合理的 inference-time ablation 冒充 training ablation。

---

## P7：Scaling

至少两个模型规模。

---

## P8：Data efficiency

至少：

```text
1%
10%
25%
50%
100%
```

---

## P9：Cross-domain / OOD

至少：

```text
in-domain
unseen layout
unseen website
structural conflict
```

---

## P10：统计与最终图表

最终生成：

1. Main benchmark table
2. MLLM comparison
3. Structural reasoning table
4. Conflict intervention figure
5. Anchor permutation curve
6. Scaling curve
7. Data efficiency curve
8. Efficiency table
9. Ablation table

---

# 24. 最终成功标准

这次实验升级不要以：

> “WebGAP 所有 benchmark 都第一”

作为成功标准。

真正成功的标准是：

### 标准 1

不再存在大量：

> 100.0 / 100.0 / 100.0

的主表。

---

### 标准 2

至少有一个真实公开 benchmark 可以有效区分：

```text
Frozen
LoRA
GraphToken
WebGAP
```

---

### 标准 3

至少有一个 structural benchmark 能够证明：

> 当前 MLLM 在网页结构理解上存在系统性问题。

---

### 标准 4

至少有一个 intervention experiment 能证明：

> WebGAP 使用的是结构信息，而不是单纯增加参数。

---

### 标准 5

至少两个不同 MLLM family 上验证核心现象。

---

### 标准 6

至少一个 OOD / cross-site evaluation。

---

### 标准 7

不能隐藏 WebGAP 输给 GraphToken 的结果。

而应该解释：

> GraphToken 在 explicit structural serialization 上具有优势，而 WebGAP 的优势需要在结构冲突、结构干预、参数效率或 cross-model generalization 中体现。

---

### 标准 8

最终论文应该能够回答三个明确的问题：

**RQ1：现代 MLLM 是否真的理解网页结构？**

**RQ2：视觉结构和 DOM 结构发生冲突时，MLLM 依赖什么？**

**RQ3：WebGAP 是否能够以更有效、更可控的方式利用结构信息？**

如果实验最终只能回答：

> “WebGAP accuracy 比 LoRA 高 1%”

那么不要继续堆实验。

如果能够回答上述三个 RQ，即使 WebGAP 在某些 benchmark 上不是第一，也更接近 WWW 级别的研究工作。

---

# 25. 工程要求

所有新增实验必须：

1. 不破坏现有代码。
2. 不覆盖已有 checkpoint。
3. 所有实验保存 config。
4. 所有结果保存 JSON。
5. 所有 benchmark 保存 metadata。
6. 所有结果可以通过统一 script 重跑。
7. 不手工修改最终数字。
8. 不删除失败实验。
9. 不为了提高/降低指标修改 evaluation protocol。
10. 所有 benchmark 必须记录官方来源。
11. 不下载超过 250GB 的数据。
12. 对每一个新增 benchmark，先记录大小再决定是否完整下载。
13. 如果某 benchmark 无法合法下载或 license 不允许当前用途，跳过。
14. 如果某模型无法公平输入相同信息，必须明确记录，而不是强行比较。
15. 对所有新模型记录：

    * model name
    * parameter count
    * checkpoint
    * precision
    * GPU memory
    * inference time
    * prompt
    * generation parameters

最终在：

```text
docs/NEXT_EXPERIMENTS.md
```

中记录完整实验计划。

同时生成：

```text
outputs/benchmark_survey/
outputs/mllm_baselines/
outputs/structural_eval/
outputs/intervention/
outputs/scaling/
outputs/data_efficiency/
```

并确保所有实验可以通过统一入口重新运行。

# 最终目标

不要把这个项目继续做成一个“synthetic webpage QA + attention plugin”的工程项目。

下一阶段应该把它升级成：

> **MLLM Web Structural Reasoning + Visual-DOM Conflict + Structure-aware Multimodal Reasoning**

其中：

* public benchmarks 提供真实性
* structural evaluation 提供研究问题
* intervention 提供因果证据
* cross-model evaluation 提供普适性
* OOD evaluation 提供泛化性
* WebGAP 提供方法创新
* failure cases 提供 scientific insight

优先级始终是：

**Benchmark quality > Research question > Experimental design > Baseline coverage > Method tuning > 指标本身。**

不要为了让 WebGAP 排名第一而修改实验。

应该让实验本身能够回答一个 WWW 审稿人真正关心的问题。
