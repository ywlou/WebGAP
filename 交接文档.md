# WebGAP 交接文档（迁移后请先读本文）

**写给未来的自己 / 新会话里的 Agent。** 读完本文就能接着干活，不必先把 `docs/` 全部翻一遍。更细的表、图、原始 json 仍以文末「文档地图」为准。

| 字段 | 值 |
| --- | --- |
| 写就日期 | 2026-09-11（原文）；2026-09-17 00:40 4080 到期交接；2026-09-17 22:50 续跑 P4；**2026-09-18 22:15 P4 收口，开 P6** |
| 当时机器 | 单卡 NVIDIA A800 80GB，仓库在 `/data/WebGAP` |
| 当前机器 | 单卡 **NVIDIA A100-SXM4-40GB**（40GB）。仓库仍在 `/data/WebGAP`。conda `torch2.4_cuda12.1`（PyTorch 2.6+cu124） |
| 底座权重 | **只放 `/data/models/`，禁止写入仓库**。GitHub **不会**带上权重、公开集、`outputs/runs/` |
| 当前停在 | **P0–P5 完成。P6 受控评测已齐（负结果）。P8 进行中。** 不要重跑 P3/P4/P5，不要给 dual_cg 开公开 P4。P6：`webgap_dual_cg` 的 hard_struct `dom` **1.6%**（原 dual 9.6%），P5 DOM 翻转仍 0%。下一步是 `python scripts/run_p8.py`（日志 `outputs/data_efficiency/p8.stdout`）：重评已有 `*_hard_curve` 的 25/50/100/200/400 step，再补 1%/10% 小样本。 |
| 投稿目标 | The Web Conference / WWW，Graph Algorithms and Modeling for the Web |
| 不是 | 第二个项目；不写论文草稿；不把 WebForge 再当公开主榜 |

**新会话第一条指令建议：**「请先读仓库根目录 `交接文档.md`，按『当前停在』和『下一步』继续，不要重做已完成阶段。」

---

## 0. 三十秒读懂项目

WebGAP = **Web Graph-Anchored attention Plugin**：把网页异构图（DOM 拓扑、二维布局、图文对齐）注入**冻结**多模态大模型的注意力，而不是再训一个智能体。

四块模块（代码名保持英文）：

| 模块 | 做什么 |
| --- | --- |
| **STAR** | 视觉 token 盒 ↔ 区域 BBox 的 IoU 赋值（无 HTML 时 4×4 空间网格回退） |
| **GACA** | 序列与少量结构锚点之间的双向交叉注意力；只在 **prefill** 插入 |
| **TRB** | 锚点–锚点注意力上的类型加性偏置（不能做 token×token，80GB 上跑不动） |
| **ERPR** | 仅训练期：GACA 映射熵下限。本设定下几乎不触发，不是成绩来源 |

三种学法对照（必须同时保留，不许把 GraphToken 做弱或从主表拿掉）：

- **B0 Frozen**：零样本
- **B1 LoRA**：同样本、同 LoRA r=16，无图插件
- **B3 GraphToken**：把图线性化成文本拼进问题（注意力**外**做图）
- **M WebGAP**：注意力**内**做图；后来加了 `anchor_mode=visual|dom|dual`

底座默认 **Qwen3-VL-8B-Instruct**（冻结视觉+LLM + LoRA）。合成页由 **WebForge**（PIL，非 Playwright）生成。受控冲突协议叫 **WebClash**（代码分割名仍是 `hard_struct` / `medium_struct`）。

---

## 1. 当前研究计划（一篇论文要讲什么）

第一轮已经证明「插件能跑、锚点被用到了」，但**没有**证明「插件比 LoRA / GraphToken 更优」。下一阶段不要堆更多合成短答，要把问题升级成：

> 多模态大模型在普通网页短答上已经很强；当**视觉几何**与**潜伏 DOM 结构**不一致时，结构归因是脆弱的。公开网页基准证明这一点仍然存在；受控干预证明模型（以及当前几何锚点插件）默认站在像素一边；GraphToken 对照证明「把结构写进 prompt」有时比「写进注意力」更能带上源序。然后把 WebGAP 从单路几何图升级为**冲突可感知的双结构融合**，并在公开 grounding / DOM 选择 / 受控冲突上检验它何时有用、何时失败。

三个 RQ（成功标准**不是** WebGAP 全榜第一）：

| RQ | 问题 | 主要证据 |
| --- | --- | --- |
| RQ1 | 现代 MLLM 是否真的理解网页结构？ | 公开：VisualWebBench **官方七类**、ScreenSpot-v2 Web、Mind2Web 元素准确率、WebSRC **POS**；跨家族零样本 |
| RQ2 | 视觉几何与 DOM 冲突时信哪一边？ | 受控：WebClash 探针、DOM/视觉重排、锚点降解；公开：Mind2Web 上量视觉序–DOM 序不一致度再分层 |
| RQ3 | WebGAP 是否更有效、更可控地使用结构？ | 同输入 Frozen / LoRA / GraphToken / WebGAP；data-efficiency；跨家族适配器；B5 因果 |

论文四张主表（数据来源已锁定，见 `docs/BENCHMARK_SURVEY.md`）：

| 表 | 内容 | 数据 |
| --- | --- | --- |
| Table 1 Public | VWB 官方均分+分任务；ScreenSpot-v2 Web；Mind2Web 元素准确率 | 别人的公开集 |
| Table 2 Structural | WebSRC POS；Mind2Web element acc；hard_struct 的 visual/dom/bind/cell | 公开 + 已有受控 |
| Table 3 Cross-model | 同一套 Table 1/2，换 MLLM 家族 | **先零样本 zoo，不要一上来给所有模型接插件** |
| Table 4 Intervention | 原页 / DOM 重排 / 视觉重排 / 冲突 / 锚点打乱 | **仅 WebForge**，诚实标注 synthetic controlled |

**硬约束：** 单卡 A800 80GB；新增公开数据预算 ≤250GB（P1 实际约 9GB）；不允许为了降分而改 GT、剔容易题、换恶心 prompt、自建一套冒充公开榜。

---

## 2. 已经做了哪些事情

### 2.1 工程（可复现代码，不是论文）

- Conda 环境 `torch2.4_cuda12.1`，实际是 **PyTorch 2.6 + CUDA 12.4**、`transformers` 5.x。
- 代码在 `src/webgap/`：数据（WebForge / 图 / STAR / collator / `html_dom.py`）、模型（`plugin.py` GACA/TRB/ERPR + `wrapper.py` Qwen3-VL）、训练、评测。
- 主配置 `configs/default.yaml`（路径写死为 `/data/WebGAP/...`，迁机后要么保持同路径，要么全局替换）。
- 流水线：`scripts/run_all.py`（可断点）、`scripts/run_p0.py`（双序翻盘，已跑完）。
- 新评测入口：`scripts/evaluate.py`（P2），结果写 `outputs/mllm_baselines/`，**不覆盖** `outputs/runs/`。
- 权重：**必须**在 `/data/models/`（默认 `/data/models/Qwen/Qwen3-VL-8B-Instruct`，约 17GB）。禁止把底座权重放进 `/data/WebGAP`。检查点：`outputs/runs/<run>/checkpoint/`（每个约 98MB，LoRA+插件，不含 8B 基座）。下载其它家族：`python scripts/download_mllm.py internvl35-8b`。
- 相对原始 `研究方案.md` 的偏离见 `docs/DESIGN_DECISIONS.md`（删 WebQA、TRB 改锚点图、放弃 vLLM、InternVL hidden 3584≠4096 等）。

### 2.2 第一轮实验（合成短答 SFT）

协议：3000 条 WebForge QA × 2 epoch，累积 8 → 约 750 step；greedy；短答 EM / token F1。

| # | 实验 | 结论（必须按这个读） |
| --- | --- | --- |
| 1 | 主 SFT：WebGAP / LoRA / GraphToken | 都能把损失打到接近 0；可训练约 29.9M |
| 2 | 易模板 held-out 500 | SFT 后 LoRA=WebGAP=**100%**，**触顶，不能排序方法** |
| 3 | H1–H5 / StHR | SFT 后五类全满，无分辨率 |
| 4 | 泄漏模板 200 | SFT 后两边 100%，测不出记忆 |
| 5 | 推理期 −GACA / −TRB | 易页仍 100%；hard 上 −GACA **略升**（72.2 vs 70.5） |
| 6 | 重训 −ERPR | held-out 99.8% vs 100%；ERPR 损失几乎一直为 0 |
| 7 | **B5 随机 STAR** | held-out 100→**26.6%**，hard 70.5→**10.5%**。几何锚点被用到了 |
| 8 | **hard_struct 不再额外 SFT** | GraphToken **78.0** > LoRA 72.0 > WebGAP 70.5；`dom` 探针只有 GraphToken 到 **33%**，其它 ≈0 |
| 9 | hardmix 约 100 step | 三者都约 99%，`dom` 全 100% → 冲突主要是**监督**问题 |
| 10 | 公开截图（旧协议） | VWB 短答 EM：冻结 18.3 > WebGAP 16.7 > LoRA 15.3（负迁移）；WebSRC EM ≈92% 饱和。**该 VWB 协议已作废** |
| 11 | 效率 12 页 | 延迟 **+6.8%**，显存 +0.05GB |

一句话：**插件确实在用几何锚点（B5）；易页过易；视觉≠DOM 时把图写进 prompt 比只插注意力更有效；一旦有冲突标注，LoRA 就够。**

检查点目录名：`outputs/runs/{webgap,lora,graphtoken}_sft/`、`webgap_no_erpr`、`webgap_hardmix`、`lora_hardmix`、`graphtoken_hardmix`、`webgap_rand_anchor`、`webgap_no_gaca`、`webgap_no_trb`、`zeroshot`。

### 2.3 双序翻盘（旧脚本 `run_p0.py`，已跑完）

针对「证明了插件被使用、还没证明插件更优」。hard_struct **1500** 题：

| 方法 | EM | visual | **dom** |
| --- | --- | --- | --- |
| Frozen | 66.7 | 73.1 | 2.6 |
| LoRA | 72.0 | 100 | 0.6 |
| GraphToken | **78.7** | 97.2 | **35.4** |
| M 视觉区域 | 70.6 | 90.5 | 0.0 |
| M DOM 锚点 | 71.1 | 93.3 | 2.6 |
| M 双序门控 | 70.9 | 84.6 | **9.6** |
| M 双序+冲突混合 | 70.9 | 87.2 | 0.0 |
| M SSL→SFT | 72.6 | 99.2 | 13.5 |
| B5 打乱视觉 STAR | 11.7 | 13.3 | 1.3 |
| B5 双序只打乱视觉 | 17.8 | 11.8 | 0.0 |

读法：双序把 `dom` 从 0 拉到 9.6（SSL 后再到 13.5），方向对，**仍远低于 GraphToken 的 35.4**。`dual_mix` 把 `dom` 打回 0。中档 `medium_struct` 800 题全体 89–98%，又一层饱和。公开集上负迁移没被 SSL 救回来。

对应检查点：`outputs/runs/webgap_{dom,dual,dual_mix,ssl,ssl_sft,dual_rand_vis}/`。双序实现：`plugin.py` 里 `anchor_mode` + token-wise `mix` 门控；B5 只打乱视觉支路。

### 2.4 评测空间升级（与第一轮合成一篇论文，不是新项目）

阶段编号以 `docs/NEXT_EXPERIMENTS.md` 第三节为准（文末第七节「下一动作」已过时）。

| 阶段 | 状态 | 要点 |
| --- | --- | --- |
| **P0** 公开基准调研 | **完成** | `docs/BENCHMARK_SURVEY.md`、`outputs/benchmark_survey/catalog.json` |
| **P1** 下载五件评测集 | **完成** | 约 9GB，**未下 train**。清单见下 |
| **P2** 统一评测接口 | **完成（可跑）** | `scripts/evaluate.py` |
| **P3** MLLM 零样本 zoo | **完成** | Qwen3-VL-8B、InternVL 8B/14B、LLaVA-OV-7B、MiniCPM-V-4.5 五件公开集已齐。30B-A3B 在 32GB 卡上放弃。**不要重跑** |
| **P4** 同空间重跑四种方法 | **完成（2026-09-18）** | Frozen / LoRA / GraphToken / WebGAP visual·dom·dual **各七件齐**。数字见第 2.7 节。不要重跑 |
| **P5** 结构干预 | **完成** | 数据 `outputs/intervention/data/`（822 条）。Frozen / LoRA / GraphToken / WebGAP visual·dom·dual 的 `metrics.json` 已齐。**不要重跑** |
| **P6** 冲突感知融合 + 消融 | **进行中** | `plugin.conflict_gate`：用 STAR 两路的期望序差 + 图级相关，加到现有 `mix` 上。训练 `webgap_dual_cg`（从 dual 续）。不要给其它家族接插件。ERPR 仍进附录 |
| P7 Scaling | **部分完成** | InternVL 8B vs 14B 零样本已齐。30B 在 32GB 上放弃 |
| P8 Data efficiency | 未开始 | `hard_train` 上 1/10/25/50/100%；已有 25/50/100/200/400 step 可先重评 |
| P9 OOD | **部分完成** | P3 的 Mind2Web `test_website` vs `test_domain` 已是 cross-site 列 |
| P10 统计与图 | 未开始 | 3 seeds、paired bootstrap、英文轴标签图 |

P1 已落地（`outputs/benchmark_survey/p1_download_summary.json`）：

| 集 | n | 磁盘约 | 路径 |
| --- | --- | --- | --- |
| VisualWebBench 全量 | 1536 | 0.28GB 整理后 / 原包更大 | `data/benchmarks/visualwebbench/full.jsonl` |
| WebSRC 官方 DEV subset | 1697 QA / 849 页 | 1.3GB | `data/benchmarks/websrc_official/eval_dev_subset.jsonl`（2 QA/页，seed 42，**不是** hidden test） |
| ScreenSpot-v2 | 1272（web **437**） | 2.8GB | `data/benchmarks/screenspot_v2/eval.jsonl` |
| GUIAct web-single test | 1410（有 bbox 1089） | 0.76GB | `data/benchmarks/guiact_web_single/eval.jsonl` |
| Multimodal-Mind2Web test | 6418（图 6407） | 4.3GB | `data/benchmarks/mind2web/{test_website,test_task,test_domain}/`；**未下 train** |

**明确没下、也不要下：** WebArena Docker、VisualWebArena、WebLINX-full、Mind2Web raw 300GB、SeeClick 130GB、WebSight、为降分自制榜。

P2 任务类型（禁止用短答 EM 去打 grounding / caption）：

| `--benchmark` | 指标 |
| --- | --- |
| `visualwebbench` | 官方 Rouge / 八选一，**不用**短答 EM |
| `screenspot_v2` | 点是否在 GT 框内；主报 web |
| `guiact` | 点∈框；无框项跳过 |
| `websrc` | EM/F1 + span-aligned **POS** |
| `mind2web` | 下一步动作字符串 vs `target_action_reprs`（**不是** full task SR，正文必须写明） |

### 2.5 P3 已有数字（零样本 greedy；权重均在 `/data/models/`）

目录：`outputs/mllm_baselines/`。以各 `*_metrics.json` 为准。Mind2Web 是下一步动作字符串匹配，**不是** Task SR / 官方 element acc。

#### Qwen3-VL-8B-Instruct（完整）

路径：`/data/models/Qwen/Qwen3-VL-8B-Instruct`。结果：`qwen3vl8b_zeroshot/`。

| 基准 | n | 主指标 |
| --- | ---: | --- |
| VisualWebBench 官方七类 | 1536 | official_avg **79.75** |
| ScreenSpot-v2 web | 437 | point-in-bbox **91.08%** |
| WebSRC official DEV subset | 1697 | EM 86.7 / F1 90.4 / **POS 77.5** |
| GUIAct web-single（有框） | 1089 | point-in-bbox **76.31%**（跳过无框 321） |
| Mind2Web test_website | 1019 | element_em 0.0 / action_f1 **21.3%** |
| Mind2Web test_task | 1338 | element_em 0.0 / action_f1 **21.6%** |
| Mind2Web test_domain | 4050 | element_em 0.0 / action_f1 **24.1%**（10 条缺图跳过） |

VWB 分任务 primary：caption Rouge-L 29.0，webqa 88.0，heading 74.9，element OCR 95.6，element ground 93.0，action pred 90.4，action ground 87.4。

#### InternVL3.5-8B-HF（完整；thinking 关闭）

路径：`/data/models/OpenGVLab/InternVL3_5-8B-HF`。结果：`internvl35_8b_zeroshot/`。生成时 ban `<think>` token + 非 thinking system prompt，与 Instruct greedy 表对齐。

| 基准 | n | 主指标 |
| --- | ---: | --- |
| VisualWebBench 官方七类 | 1536 | official_avg **72.62** |
| ScreenSpot-v2 web | 437 | point-in-bbox **73.91%** |
| WebSRC official DEV subset | 1697 | EM 86.3 / F1 90.3 / **POS 77.6** |
| GUIAct web-single（有框） | 1089 | point-in-bbox **49.40%** |
| Mind2Web test_website | 1019 | element_em 0.0 / action_f1 **9.2%** |
| Mind2Web test_task | 1338 | element_em 0.0 / action_f1 **7.5%** |
| Mind2Web test_domain | 4050 | element_em 0.0 / action_f1 **10.5%** |

#### InternVL3.5-14B-HF（**完成**；2026-09-15 在 RTX 4080 SUPER 32GB 上续跑 GUIAct + Mind2Web）

路径：`/data/models/OpenGVLab/InternVL3_5-14B-HF`（约 29GB）。bf16 评测峰值约 30GB。结果：`internvl35_14b_zeroshot/`。

| 基准 | n | 主指标 |
| --- | ---: | --- |
| VisualWebBench 官方七类 | 1536 | official_avg **75.18** |
| ScreenSpot-v2 web | 437 | point-in-bbox **89.02%** |
| WebSRC official DEV subset | 1697 | EM 86.3 / F1 82.7 / **POS 71.7** |
| GUIAct web-single（有框） | 1089 | point-in-bbox **75.67%**（跳过无框 321） |
| Mind2Web test_website | 1019 | element_em 0.0 / action_f1 **18.4%** |
| Mind2Web test_task | 1338 | element_em 0.0 / action_f1 **19.4%** |
| Mind2Web test_domain | 4050 | element_em 0.0 / action_f1 **22.3%** |

**读法：** 旧协议把 VWB 打成短答 EM 18% 是指标错配。Qwen3-VL-8B 在官方 VWB / ScreenSpot-v2 Web 上已经很高；InternVL-8B 明显更弱（尤其 GUIAct 49 vs 76）。14B 把 ScreenSpot 从 73.9 拉到 89.0、GUIAct 从 49.4 拉到 75.7，接近 Qwen-8B。WebSRC POS 14B（71.7）反而低于 8B（77.6），不要写成单调 scaling。Mind2Web 字符串精确匹配几乎全是 0（模型输出短动作串，官方是 `[div] foo -> CLICK`），action F1 才有分辨率；14B 介于 InternVL-8B 与 Qwen-8B 之间。P4 前若要更贴官方，应改为选节点 id，不要用短字符串 EM 冒充 element acc。

#### LLaVA-OneVision-7B（HF conversion；**完成**；2026-09-15）

路径：`/data/models/llava-hf/llava-onevision-qwen2-7b-ov-hf`。结果：`llava_ov_7b_zeroshot/`。

| 基准 | n | 主指标 |
| --- | ---: | --- |
| VisualWebBench 官方七类 | 1536 | official_avg **55.81** |
| ScreenSpot-v2 web | 437 | point-in-bbox **4.35%** |
| WebSRC official DEV subset | 1697 | EM 79.8 / F1 85.1 / **POS 74.9** |
| GUIAct web-single（有框） | 1089 | point-in-bbox **2.39%**（跳过无框 321） |
| Mind2Web test_website | 1019 | element_em 0.0 / action_f1 **12.4%** |
| Mind2Web test_task | 1338 | element_em 0.0 / action_f1 **11.1%** |
| Mind2Web test_domain | 4050 | element_em 0.0 / action_f1 **13.8%** |

ScreenSpot / GUIAct 接近地板，像坐标格式对不上（不是能力真那么差）。QA/OCR 侧 VWB 55.8、WebSRC POS 74.9 仍可用。不要为了抬 grounding 改提示后重跑已写盘的 LLaVA 表。

#### MiniCPM-V-4.5（**完成**；2026-09-15）

路径：`/data/models/openbmb/MiniCPM-V-4_5`。结果：`minicpm_v45_zeroshot/`。`load_minicpm` 对 transformers 5.x 缺 `all_tied_weights_keys` 打了补丁；thinking 关闭。

| 基准 | n | 主指标 |
| --- | ---: | --- |
| VisualWebBench 官方七类 | 1536 | official_avg **76.40** |
| ScreenSpot-v2 web | 437 | point-in-bbox **63.39%** |
| WebSRC official DEV subset | 1697 | EM 80.1 / F1 81.5 / **POS 78.8** |
| GUIAct web-single（有框） | 1089 | point-in-bbox **51.70%**（跳过无框 321） |
| Mind2Web test_website | 1019 | element_em 0.0 / action_f1 **20.3%** |
| Mind2Web test_task | 1338 | element_em 0.0 / action_f1 **21.1%** |
| Mind2Web test_domain | 4050 | element_em 0.0 / action_f1 **24.4%** |

零样本能力榜（VWB）：Qwen-8B 79.8 > MiniCPM 76.4 > InternVL-14B 75.2 > InternVL-8B 72.6 > LLaVA 55.8。WebSRC POS 上 MiniCPM **78.8** 最高。Mind2Web action F1 上 MiniCPM 与 Qwen-8B 接近（domain 24.4 vs 24.1）。不要重跑。

### 2.6 本轮工程（2026-09-13）

- 底座权重从 `/data/WebGAP/checkpoints/Qwen3-VL-8B-Instruct` **迁到** `/data/models/Qwen/Qwen3-VL-8B-Instruct`。仓库内不再放 8B/14B 权重。默认路径：`WEBGAP_MODELS_DIR=/data/models`（见 `src/webgap/constants.py`）。
- 新增 `scripts/download_mllm.py`（只下载到 `/data/models/`）、`scripts/run_p3_zoo.py`（一套公开集串行评一个模型）。
- `scripts/evaluate.py` 支持 `--model-path` / `--family auto|qwen3vl|internvl|llava_ov|minicpm`。InternVL 用 `generate_internvl`（`apply_chat_template(..., tokenize=True)` + 禁止 thinking）。
- 仓库里仍有 `checkpoints/hf_home` 约 24GB **数据集** HF 缓存（不是底座权重），以及 `.hf_cache` 约 9GB。需要腾盘时可删，不影响已整理的 `data/benchmarks/`。

### 2.7 P4 已有数字（同 Qwen3-VL-8B、同一 `evaluate.py`；**已齐**）

目录：`outputs/mllm_baselines/{lora,graphtoken,webgap_sft,webgap_dom,webgap_dual}_public/`。Frozen 列就是 P3 的 `qwen3vl8b_zeroshot`。

| 方法 | VWB 官方均 | ScreenSpot | WebSRC POS（EM） | GUIAct | Mind2Web website / task / domain（动作 F1） |
| --- | ---: | ---: | ---: | ---: | ---: |
| Frozen | 79.75 | **91.08** | 77.5（86.7） | **76.31** | 21.3 / 21.6 / 24.1 |
| LoRA | 81.07 | **91.30** | **80.7**（86.7） | **76.31** | 20.5 / 18.5 / 22.6 |
| GraphToken | 80.10 | 84.90 | 80.3（85.9） | 74.29 | **28.7 / 28.6 / 29.6** |
| WebGAP visual | 81.72 | 73.23 | 79.0（88.0） | 69.51 | 22.5 / 21.6 / 26.8 |
| WebGAP dom | 80.00 | 60.87 | 79.7（88.1） | 61.98 | 19.4 / 19.9 / 24.2 |
| WebGAP dual | **81.74** | 33.41 | 77.8（85.6） | 35.08 | 21.8 / 20.1 / 25.5 |

读法（主结果，不要写成全面 SOTA）：

- **跨家族零样本（P3）**：Qwen Frozen 已经是五家里 grounding / VWB 最强；MiniCPM 的 WebSRC POS 最高（78.8）。没有给 InternVL / LLaVA / MiniCPM 接插件。
- **同底座方法（P4）**：VWB 上 dual/visual 略高。ScreenSpot / GUIAct 上插件 **明显退步**，dual 更差（91→33，76→35）。POS 上 **LoRA 最好**。Mind2Web 动作 F1 上 **GraphToken 明显最好**；visual domain 26.8 介于 Frozen 与 GraphToken 之间。元素 EM 全是 0，不能当 element acc。
- 与旧 `hard_struct` 一致：**不得删 GraphToken > WebGAP**；公开 grounding 不是 WebGAP 赢。这是符合改写后 RQ 的失败形态，不是意外打脸。

### 2.8 P5 已有数字（RQ2；合成成对页，已齐）

数据：`outputs/intervention/data/index.jsonl`（822 行）。visual_reorder 48 对（只翻 visual gold）、dom_reorder 48 对（只翻 DOM gold）、conflict 32、curve 32。结果：`outputs/intervention/<method>/metrics.json`。

「跟上」= gold 翻转且两臂预测都等于各自 gold。

| 方法 | 视觉序翻转跟上 | DOM 序翻转跟上 | 冲突页 visual EM | 锚点曲线（identity → global） |
| --- | ---: | ---: | ---: | --- |
| Frozen | **0%** | 0% | 30% | 仅 identity 31%（无 STAR perm） |
| LoRA | 50% | 0% | 93% | identity 98% |
| GraphToken | 8% | 0% | 76% | identity 95% |
| WebGAP visual | **100%** | 0% | **97%** | 100% → local 91 / near 94 / **global 12.5** / random 25 |
| WebGAP dom | **100%** | 0% | 97% | 视觉 perm 全是 100%（DOM 模式不吃视觉打乱） |
| WebGAP dual | **100%** | 0% | 87% | 94% → **global 12.5** |

读法：WebGAP 是唯一稳定跟上**视觉结构**干预的方法；**DOM 序干预谁都没跟上**（包括 `webgap_dom`）。Frozen 预测乱跳但不跟 gold。这是目前最能支撑「插件用了结构」的一段，不能用 Mind2Web 假装替代。

### 2.9 P6 dual+conflict-gate（已齐；负结果）

检查点 `outputs/runs/webgap_dual_cg/`（从 dual 续 1 epoch × 1500）。公开 P4 **不要**为抬分去跑。

| | hard EM | visual | **dom** | P5 视觉跟上 | P5 DOM 跟上 | identity → global |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| WebGAP dual | 70.9 | 84.6 | **9.6** | 100% | 0% | 94 → 12.5 |
| **dual+CG** | 68.1 | 84.4 | **1.6** | 100% | 0% | 95 → **0** |

只在 `mix` 上加序差**没有**让模型在冲突时改信 DOM，还更偏视觉。正文保留；不要再混 `hard_train` 重训一版来刷 `dom`。

---

## 3. 明确没做 / 到期时没跑完（迁机后按这个补，不要重做已齐的）

**P6 已齐（负结果），不要重跑。P8 正在做：**

- `scripts/run_p8.py`：LoRA / GraphToken / WebGAP dual 的 `hard_curve` 在 hard_struct 上评 checkpoint-0/25/50/100/200/400（n=400），再训 1%（8 条）和 10%（80 条）。
- 结果 `outputs/data_efficiency/points.json`。日志 `outputs/data_efficiency/p8.stdout`。不要 `tail -n` 这份日志（tqdm 几乎无换行）。
- 不要给 `webgap_dual_cg` 开公开七件。

**取舍（不是漏跑，不要擅自开做）：**

- 不写论文草稿；不把 WebForge 当公开主榜。
- 不把 WebQA 当网页结构集。
- 不做 FastV（B4）、多种子重训、完整长度×batch 效率曲线。
- 默认主 SFT 不跑阶段一 SSL（代码有 `--stage ssl`；后来为翻盘跑过 `webgap_ssl` / `webgap_ssl_sft`）。
- **不要**给 InternVL / LLaVA / MiniCPM 接插件（hidden 不同，需要投影）。
- **不要**下 Qwen3-VL-30B-A3B；32GB 卡装不下完整评。
- 不要为抬 LLaVA grounding 改提示后重跑已写盘的 LLaVA 表。
- P6 冲突门控已训已评，**负结果**（见第 2.9 节）。公开 dual+CG 不要跑。
- P8 data-efficiency：`run_p8.py`。P10 多种子 / bootstrap / 论文图：未开始。
- Mind2Web 仍是下一步动作**字符串** F1，不是官方 Element Acc / Task SR。

---

## 4. 下一步（P6 已齐：跑 P8，不要重做）

**2026-09-19：P6 负结果已写盘。P8 用 `scripts/run_p8.py`。新会话先看有没有 `run_p8.py`/`eval.py`/`train.py`：有就只监控。不要重跑 P3/P4/P5/P6，不要给其它家族接插件。**

### 0. 离机前必须拷走（GitHub 不够）

`.gitignore` 排除了数据集、底座、`outputs/runs/`。代码已有 remote `https://github.com/ywlou/WebGAP.git`（commit `48f77d3` 是迁机备份；**本机还有大量未提交改动**，包括 `evaluate.py` 建图、`scripts/run_p4_zoo.py` / `eval_p5.py` / `run_p45.py`、P3/P4/P5 指标）。**不要只 clone 旧 commit 就开跑。**

| 优先级 | 路径 | 约大小 | 为什么 |
| --- | --- | --- | --- |
| 必拷 | 整个 `/data/WebGAP`（至少 `src/` `scripts/` `configs/` `docs/` `交接文档.md`） | 代码小 | 图路径、P4/P5 脚本都在未提交改动里 |
| 必拷 | `outputs/mllm_baselines/` | ~19MB | P3 全套 + P4 已完成件；`run_p4_zoo.py` 靠 `*_metrics.json` skip |
| 必拷 | `outputs/intervention/` | ~15MB | P5 数据 + 六套 metrics |
| 必拷 | `outputs/runs/*/checkpoint/` | 每套 ~98MB，合计进 4.6GB 目录 | 不拷就要重训才能续 P4 |
| 必拷 | `data/benchmarks/` | ~9GB | 比重新 `download_p1.py` 快 |
| 必拷 | `/data/models/` | Qwen 17GB + InternVL 8B/14B + LLaVA + MiniCPM | 禁止放进仓库 |
| 可后补 | `outputs/runs/` 里的 preds / 旧 eval json | — | 第一轮数字已写进本文第 2.2 / 2.3 |
| 可删 | `checkpoints/hf_home`、`.hf_cache` | 数十 GB | 不是底座；有 `data/benchmarks/` 即可 |

公开集 WebGAP **必须** `model.set_graph`（已在 `evaluate.py` + `src/webgap/eval/public_graph.py`）。没有这份代码就不要 `--ckpt` 开跑。

### 1. P8（当前）

```bash
pgrep -af 'run_p8|scripts/eval.py|scripts/train.py'
# 日志几乎无换行，不要 tail -n；看最后几百字节：
tail -c 600 outputs/data_efficiency/p8.stdout | tr '\r' '\n' | tail -n 15

conda activate torch2.4_cuda12.1
export PYTHONPATH=/data/WebGAP/src HF_ENDPOINT=https://hf-mirror.com
export WEBGAP_MODELS_DIR=/data/models HF_HOME=/data/models
export PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True
cd /data/WebGAP
mkdir -p outputs/data_efficiency
nohup python -u scripts/run_p8.py >> outputs/data_efficiency/p8.stdout 2>&1 &
```

已有 `p8_hard_struct_metrics.json` 的点会 skip。不要重跑 P3/P4/P5/P6。不要下 30B。不要给其它家族接插件。

### 2. P8 数字出来之后

| 阶段 | 状态 | 能不能马上跑 |
| --- | --- | --- |
| P3–P5 | **完成** | 不要重跑 |
| P4 公开五方法 | **完成** | 不要重跑 |
| P6 dual+conflict-gate | **完成（负结果）** | 不要重跑、不要开公开七件 |
| P8 data-efficiency | 进行中 | `run_p8.py` |
| P7 30B | 放弃 | InternVL 8B/14B 已齐 |
| P9 OOD | 部分完成 | Mind2Web website vs domain |
| P10 图与统计 | 未开始 | 等 P8 曲线 |

### 迁机环境备忘（细节见上面第 0 步）

硬编码 `/data/WebGAP` 的文件包括：`configs/default.yaml`、`scripts/evaluate.py`、`scripts/download_p1.py`、`scripts/pack_websrc_official.py`、`scripts/efficiency.py`、`scripts/eval_public.py`、`scripts/download_benchmarks.py`、`scripts/plot_results.py`、`scripts/write_report.py`、`scripts/rescore.py`、`figures/scripts/plot_results.py`。新路径要么保持 `/data/WebGAP`，要么全局替换。

Conda：`torch2.4_cuda12.1`，实际 PyTorch 2.6+cu124，`pip install -e .`。权重必须在 `/data/models/`，检查点在 `outputs/runs/<run>/checkpoint/`。**优先 rsync 检查点，不要为了迁机重训第一轮。**

---

## 5. 数字怎么用、什么不许写进主声称

面向 WWW，按「能站得住 → 失败也有信息 → 不要当主排序表」：

1. **能站得住：** B5（打乱 STAR 则坍塌）；P5 视觉重排上 WebGAP follow **100%**、Frozen **0%**，打乱视觉锚点后 identity 100% → global 12.5%。
2. **失败也有信息：** hard_struct 上 GraphToken `dom` 35.4 vs LoRA/视觉 WebGAP ≈0；P4 公开 ScreenSpot/GUIAct 上 WebGAP **低于 Frozen/LoRA**；Mind2Web 动作 F1 上 GraphToken 最好；P5 上 **DOM 序干预谁都没跟上**。源序冲突主要是监督问题；「注意力里做图」并不自动优于「prompt 里写图」。**不得删除 GraphToken > WebGAP，也不得把公开 grounding 写成 WebGAP 赢。**
3. **不要当主排序表：** 易 held-out 100%、泄漏 100%、该集上 −GACA/−TRB、medium_struct 89–98%、WebSRC 截图短答 EM 92%、旧 VWB 短答 EM 18%、Mind2Web element_em 全 0。
4. **公开集迁移（新协议）：** VWB 官方均上 WebGAP 略升；grounding 退步；POS 小幅升但 LoRA 更高。旧协议「合成 SFT 伤 VWB 短答 EM」不再当主表。
5. **工程：** +6.8% 延迟，单卡约 19GB 可训；P4 评测约 17–23GB。数据可本地生成。

不声称：线上智能体 SOTA、跨家族即插即用、WebForge 可替代公开榜、ERPR 在本设定下起了作用、TRB 的 DOM 偏置在无 HTML 的公开截图上起了作用。

---

## 6. 文档地图（读完本文再按需点开）

| 先读 | 路径 | 用途 |
| --- | --- | --- |
| **本文** | `交接文档.md` | 进度、下一步、迁机 |
| 已完成实验叙事 | `docs/OVERVIEW.md` | 每个实验为什么跑、怎么读数字 |
| 自动生成的表 | `docs/EXPERIMENTS.md` | 改评测后跑 `scripts/write_report.py` 重出，不要手改数字 |
| 方案偏离 | `docs/DESIGN_DECISIONS.md` | 相对 `研究方案.md` 改了什么 |
| 冲突基准 | `docs/WEBCLASH.md` | hard/medium 模板与探针 |
| 公开集调研 | `docs/BENCHMARK_SURVEY.md` | 下什么、不下什么 |
| 阶段计划 | `docs/NEXT_EXPERIMENTS.md` | P0–P10；**以第三节状态为准** |
| 用法/数据/方法 | `docs/USAGE.md` `docs/DATA.md` `docs/METHOD.md` | 命令与实现 |
| 原始需求（很长） | `改进要求.md` `研究方案.md` | 升级要求与最初方案；细节以本文 + NEXT_EXPERIMENTS 为准 |
| 中文备份 | `docs/zh/` | 与英文文件名那几份同步的副本 |
| 论文图 | `figures/pdf/` `figures/png/` `figures/data/*.csv` | 轴标签英文；改图用 `scripts/plot_results.py` |
| 旧 run 指标 | `outputs/runs/<run>/eval/*_metrics.json` | 第一轮原始数字 |
| P3/P4 新指标 | `outputs/mllm_baselines/` | 不覆盖旧 run；P4 未齐件见第 3 节 |
| P5 干预 | `outputs/intervention/` | 数据 + 六套 metrics；不要重跑 |

过时段落（不要按其「下一步」执行）：`docs/OVERVIEW.md` 第七节曾写「P1 尚未下载」；`docs/NEXT_EXPERIMENTS.md` 第七节曾写「当前停在 P0」。**以本文第 2.4 / 第 3 / 第 4 节为准。**

---

## 7. 关键实现备忘

- LoRA r=16，alpha=32；插件 `bottleneck=768`，`max_anchors=48`，`insert_every=2`，`share_across_layers=true`，`out_proj` 零初始化。
- `--anchor-mode visual|dom|dual`；dual 的 `mix` 门控初始偏视觉（`gate_bias_init: 2.0`）。
- P6 `--conflict-gate`：在 dual `mix(x)` 上加 token 级序差与图级相关（`cg_tok` 初值 -2，`cg_graph` 初值 -1）。关此开关时 dual 前向与 P4 检查点一致。旧 `plugin.pt` `strict=False` 加载。
- 推理期 `--no-gaca` / `--no-trb` / `--random-anchors` **不重训**。关 ERPR 必须重训。
- `hard_train` seed 77777，与评测 `hard_struct` 不相交。不要在 hard/medium 上调参。
- 无 HTML 的真实截图：STAR 退化为 4×4 网格，不能声称 DOM TRB 在生效。
- Mind2Web 当前指标是 next-action 字符串匹配，不是官方逐步 Element Accuracy / Task SR；**P4 已按字符串协议跑完 LoRA/GraphToken/WebGAP visual 的 website+task**，换官方 element id 是新工作，不要在续跑缺口时改协议。
- `scripts/run_p4_zoo.py` / `eval_p5.py`：已有 `metrics.json` 则 skip。缺文件才跑。续跑不要删已完成目录。
- `scripts/evaluate.py`：**按样本 id 续跑**。没有 `*_metrics.json` 但已有 `*_preds.jsonl` 时跳过已写 id，新预测 append 后在收尾重写整份 jsonl 并写 metrics。中断后不要删 preds。冒烟必须换 `--run-name`，否则会把 n=2 的 metrics 写成正式件导致 zoo skip。
- P4 公开截图无 HTML 时 STAR 用 4×4 `spatial_grid_nodes`；WebSRC / Mind2Web 有 HTML 则建 DOM 图。GraphToken 把图序列化进 prompt。
- `scripts/evaluate.py` 的 `BENCH` 写死 `/data/WebGAP/data/benchmarks`。
- InternVL 必须关 thinking（`generate_internvl` 已 ban `<think>`）；否则 grounding 分数会被截断的思维链打成 0。
- 14B 在 RTX 4080 SUPER 32GB 上 bf16 可评（峰值约 30GB）；30B-A3B 不要假设能装。

常用命令：

```bash
# 第一轮复现（有 GPU、有权重、有 WebForge 时）
python scripts/generate_webforge.py
python scripts/train.py --run-name webgap_sft --baseline webgap --epochs 2
python scripts/eval.py --run-name webgap_sft --ckpt outputs/runs/webgap_sft/checkpoint --split heldout --max-samples 500
python scripts/eval_hard.py --max-samples 400
python scripts/plot_results.py && python scripts/write_report.py

# 新评测空间
python scripts/evaluate.py --benchmark visualwebbench --run-name qwen3vl8b_zeroshot
python scripts/evaluate.py --benchmark screenspot_v2 --run-name qwen3vl8b_zeroshot --subset web
python scripts/evaluate.py --benchmark websrc --run-name qwen3vl8b_zeroshot
python scripts/evaluate.py --benchmark guiact --run-name qwen3vl8b_zeroshot
```

---

## 8. GitHub 备份范围（代码进 git，重量不进 git）

Remote：`https://github.com/ywlou/WebGAP.git`。已有迁机备份 commit `48f77d3`。**4080 上还有未提交的 P2–P5 代码和文档**（`evaluate.py` 建图、`public_graph.py`、`run_p4_zoo.py`、`eval_p5.py`、`generate_p5.py`、本文）。迁机前尽量把**代码+本文** commit / 另打包；**不要**指望只 clone `48f77d3` 就能续 P4。

`.gitignore` 排除数据集、权重、HF 缓存、`outputs/runs/`。`outputs/mllm_baselines/*.json` 很小，**建议拷盘或纳入备份**，否则会误重跑 P3。

迁机应：

1. 拷或拉：代码、文档、`outputs/mllm_baselines/`、`outputs/intervention/`。
2. 另拷：`/data/models/`、`data/benchmarks/`、`outputs/runs/*/checkpoint/`。
3. 新机器上按第 4 节续 `run_p4_zoo.py`。

不要把 token、`.env`、Hugging Face 登录态提交上去。
