# 公开基准调研产物

本目录只放 **P0 调研** 与后续每个基准的 plan/metadata。此时 **没有** 新的评测分数。

| 文件 | 含义 |
| --- | --- |
| `catalog.json` | 调研总表（体积、许可、P1 决策） |
| 人类可读说明 | `docs/BENCHMARK_SURVEY.md` |
| 实验计划 | `docs/NEXT_EXPERIMENTS.md` |

P1 起每个将下载的集先写 `<id>_plan.json`，下载后再写 `<id>_metadata.json`。
